import * as React from 'react';
import * as ReactDOM from 'react-dom';
import Layout from 'components/pages/Layout';

import 'assets/main.css';

ReactDOM.render(<Layout />, document.querySelector('.react-root'));
